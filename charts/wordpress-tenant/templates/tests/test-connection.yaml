apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordpress-tenant.fullname" . }}-test-connection"
  labels:
    {{- include "wordpress-tenant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordpress-tenant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
